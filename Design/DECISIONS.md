# Decisions — the *why*, condensed

One line or a short paragraph per decision. Check here before "fixing" something that looks wrong — it might be deliberate. Full narrative (if you ever need it) is in the per-library changelogs; not needed for normal work.

- **Single assembly, not a multi-package split.** `MonoPrimitives.dll`, raylib/MonoGame.Extended-style — explicit preference over separate `Core`/`Primitives2D`/`Primitives3D` packages. Folder layout (`Core/`, `2D/`, `3D/`) is unchanged; only the number of installable artifacts changed.
- **Namespaces unified under one root: `MonoPrimitives`/`MonoPrimitives.Primitives2D`/`MonoPrimitives.Primitives3D`.** Previously `Primitives2D` and `MonoPrimitives3D` were bare top-level namespaces unrelated in name to `MonoPrimitives` (Core) — inconsistent naming (one has no prefix, one has no dot) with no signal they're one package. Nesting both under `MonoPrimitives.*` makes a single `using MonoPrimitives.*;` read as "this project," matches the single-assembly decision above, and is the naming MonoGame.Extended-style libraries use for their own sub-namespaces.
- **No physics resolution, ever.** `Collision2D`/`Collision3D` detect overlaps only; response (push apart, impulses, constraints) is out of scope. A Verlet-based resolution module was built once and deleted for crossing this line.
- **Core-sharing rule:** code identical apart from the namespace goes in `Core/`. Genuinely different code (e.g. 2D vs 3D math) stays duplicated per namespace rather than forced into a shared abstraction. `SpatialHashGrid2D`/`3D` were removed entirely (out of scope), not moved to Core.
- **Antialiasing is MSAA**, a caller-side `GraphicsDeviceManager` setting (see the sample's `PreferMultiSampling`) — not something the library does internally. A screen-space shader post-process (FXAA-style, then a simpler edge-aware blend) was built and verified rendering correctly, but shelved anyway: kept making real content look worse in side-by-side comparison despite passing isolated checks, and MSAA already covers this adequately. Simplicity won over "we already built it." A feathering/`AntialiasedEdges` direction was abandoned earlier for the same reason; either could come back if there's a concrete reason MSAA falls short.
- **`Camera3D` merges camera + controller into one class**, unlike raylib's split — better fit for C#'s OO style. `Camera2D` mirrors the same input-driven-update shape (previously it had no input handling at all — every caller, including the sample, hand-rolled WASD-pan/wheel-zoom/drag-pan from scratch).
- **`Camera2D`/`Camera3D` both take their `ViewportAdapter2D` at construction, not as a per-call parameter.** First pass (2D) added `ScreenToWorld(pos, adapter)`/`WorldToScreen(pos, adapter)`/`ReadDefaultInput(dt, adapter)` overloads — functionally fine, but required passing the same adapter at every call site, and MonoGame.Extended's own `OrthographicCamera(ViewportAdapter)` doesn't work that way. Redone to match, then carried into `Camera3D` for the same reason: a `Camera2D(ViewportAdapter2D, ...)` / `Camera3D(ViewportAdapter2D, ...)` constructor stores it on a `ViewportAdapter` property, and every viewport-dependent method reads that stored reference instead of taking one as an argument — `ScreenToWorld`/`WorldToScreen`/`GetVisibleWorldBounds`/the mouse-drag part of `ReadDefaultInput` on the 2D side; `GetWorldToScreen`/`GetScreenToWorld`/`GetScreenToWorldRay` and `Primitive3DBatch.Begin(camera)` on the 3D side (the old `Begin(camera, viewportAdapter)` overload was removed — `Begin(camera)` alone now does `camera.ViewportAdapter?.Apply()` first). `Camera2D.Offset` defaults to the adapter's virtual center (Extended's convention) but stays a plain mutable field, so callers that want a different anchor (e.g. this project's own 2D gallery, which wants `Offset = Vector2.Zero` so `Target` is the world point at the screen's top-left corner) just set it back after construction — no separate code path needed.
- **`Camera3D.Reset()` + `R` binding.** Restores Position/Target/Up/Fovy/Projection/near/far to the values passed at construction and clears zoom/follow/head-bob smoothing state, bound to `R` in `UpdateWithInput`, which returns immediately on the reset frame (skips movement) so a same-frame WASD/mouse delta can't immediately fight it. `Camera2D` has no equivalent yet — not asked for on the 2D side.
- **`Primitive3DBatch.Begin(Camera3D, ViewportAdapter2D)` overload.** Plain `Begin(camera)` always projects using `Device.Viewport.AspectRatio` — correct only if nothing has narrowed the device viewport. A 2D layer letterboxed via `BoxingViewportAdapter2D` never touches `Device.Viewport` itself unless something calls `Apply()`, so a 3D scene sharing the same window as boxed 2D content would silently project against the full backbuffer and stretch into the bars. The new overload just calls `viewportAdapter.Apply()` then delegates to the existing `Begin(camera)` — no separate 3D-specific viewport math needed, since `ViewportAdapter2D`'s `BoundingRectangle` is exactly the on-screen sub-rect a 3D projection needs too. See `Guide/Camera2D_Guide.md`.
- **`PrimitiveInput` reviewed for a suspected freeze on window drag/resize/close — no bug found, not re-investigated without a repro.** Full read-through of `PrimitiveInput.cs`: no blocking calls, no loops, no window-event subscriptions of any kind (nothing hooks `Activated`/`Deactivated`/`Exiting`/`ClientSizeChanged`) — confirmed by grepping the whole `src/` tree for blocking primitives (`Thread.Sleep`, `.Wait()`, `lock`, `while(true)`) and window-lifecycle event names, both empty. The freeze this was reported against is almost certainly MonoGame/Windows' own `WM_ENTERSIZEMOVE` behavior — the OS blocks the message pump during a window drag/resize, so `Game.Tick()` (and everything downstream, including this library) simply doesn't run until the drag ends. That's outside any user-level library's control; the usual workarounds (tick from a separate thread/timer) are out of scope for a primitive-drawing library. If a concrete repro shows up that isn't just "window drag pauses like every MonoGame game," re-open this.
- **`Collision3D.GetRayCollisionBox`'s face-normal detection rewritten from fixed-epsilon to nearest-face selection.** Found during a full utilities audit (`Design/README.md`'s 5 docs don't cover this level of detail; noted here since it's non-obvious). The old code checked each of the 6 face planes with a hardcoded `1e-3` absolute epsilon, falling through to a hardcoded `+Z` normal if none matched. Empirically confirmed broken at scale: a 1,000,000-unit box with oblique rays returned the wrong (always the hardcoded fallback) normal on ~7% of hits, because floating-point error in the hit-point computation at that scale (0.03–0.12 units) routinely exceeds a fixed 0.001 epsilon even for a geometrically unambiguous mid-face hit (next-nearest face tens of thousands of units away). Fixed by computing the distance to all 6 face planes and picking the minimum — scale-invariant, no epsilon to tune. Verified: 0/2000 wrong normals after the fix, same test.
- **`Noise.Sample1D` given its own dedicated 1D gradient instead of slicing `Sample3D` at y=0,z=0.** Also found during the utilities audit. `Sample2D` slicing `Sample3D` at z=0 is fine (both x and y stay free variables, so `Grad`'s 12-direction table always has a nonzero-contributing term) — but `Sample1D` fixed BOTH y and z to 0, and several of `Grad`'s hash cases route their x-facing component through `y` or `z` instead of `x`, so those cases evaluated to exactly zero. Empirically confirmed: ~23% of samples landed within 0.01 of zero (vs ~2% for `Sample2D`, the expected rate for real noise) — an 11x inflation, i.e. visibly flatter/steppier output than the class's own docs promise ("a smooth, deterministic wander," explicitly for things like wind-gust strength). Fixed with the standard dedicated 1D gradient (±1 selected by a hash bit). Verified: near-zero rate dropped to ~3%, in line with `Sample2D`'s.
- **`PrimitiveInput.DragDelta`/`IsDragging` fixed to still work on the release frame.** `_dragStart` used to be nulled the instant a button went up — same frame `IsMouseButtonReleased` becomes true — so the natural "on release, check how far I dragged" pattern always read zero. Now `_dragStart` only resets on the *next* press; a `_dragEnd` snapshot is captured once, right at release, so `DragDelta` keeps reporting the completed drag's distance until the next press starts a new one. `IsDragging` explicitly requires `IsMouseButtonDown` too, so it doesn't inherit that persistence (its own doc says "while held").
- **`IsMouseButtonDoubleClicked` now also checks `DoubleClickDistance` (default 6px), not just `DoubleClickTime`.** Previously two clicks anywhere on screen within the time window counted as a double-click.
- **`Easing` given every family's In/Out/InOut** (`QuartInOut`, `ExpoInOut`, `BackInOut`, `BounceIn`/`InOut`, `ElasticIn`/`InOut`) — previously inconsistent (Quad/Cubic/Sine had all three, others didn't), standard Penner formulas.
- **2D's private `SampleUnitCircle` promoted to a public `UnitCircleLut` class**, matching 3D's `TrigLut` being public — a 2D user had no equivalent fast-trig tool for their own curved geometry. `Primitive2DBatch` now delegates to it instead of keeping a duplicate table. `UnitCircleLut`'s 512-entry table and single `Sample(t01)` API and `TrigLut`'s 1024-entry table and `SinIndex`/`CosIndex`/`SinCosStep` API are left genuinely different, not unified: 2D's circles are typically screen-sized (hundreds of pixels), 3D's are frequently much larger world-space radii needing finer angular resolution, and `TrigLut`'s split sin/cos accessors exist because 3D's sphere/cylinder/torus tessellation samples sin and cos at unrelated angles far more often than 2D's simpler circle walk does. Matching APIs here would mean forcing one's use pattern onto the other for a resemblance with no real benefit.
- **2D's ray checks (`Collision2D.CheckCollisionRay*`) return `bool` + `out` params; 3D's (`Collision3D.GetRayCollision*`) return a `RayCollision3D` struct.** Deliberately not unified into one shape: a 3D raycast commonly needs to compare `Distance`/`Normal` across several candidate hits (which face of a box, which of many objects is nearest) before deciding what "the" hit is, so bundling the result into one reusable, comparable struct pays for itself; 2D's checks are almost always used as an immediate yes/no gate (did the mouse click this circle) where the extra allocation-free `out` params are already the lighter-weight fit. Naming follows the same split: `Check*` for the 2D yes/no gate, `Get*` for 3D's "get me the result struct."
- **`Palette.GradientPairs` added**: inner/outer color pairs for `FillCircleGradient`'s glossy "juicy ball" look, a different flavor from the flat UI colors above. User-supplied palette code (a "fruit merge" game's ball colors) was the direct inspiration; a curated 6-entry subset for hue variety, not the full set — meant to grow, not meant as the final word on which colors.
- **`Fill*Shadow` (drop shadows) built as one vertex-colored ring around the shape's own boundary, not literal 9-slice (9 independently-scaled pieces).** A shader-based blur was ruled out already (see the antialiasing entry below — same reasoning: keep it simple, no shader). The ring approach: solid shape fill (reusing `FillRectangleRounded`/`FillRectangleChamfer`/`FillCircleGradient` exactly, so there's never a seam) plus a halo built from `OutsetConvexPolygon` (new — `InsetConvexPolygon`'s mirror image, same miter-offset math, picks the point *farther* from centroid instead of closer) tracing the shape's own already-generated boundary (`GenerateRoundedRectBoundary`/`GenerateChamferRectBoundary`) outward by `spread`, opaque at the boundary fading to alpha-0 at the outer edge. Because it traces the real boundary instead of scaling fixed corner/edge sprites, it automatically supports per-corner radius, rotation, and chamfer with zero extra bookkeeping — whatever `FillRectangleRounded` can draw, its shadow matches exactly. `FillCircleShadow` is a thin `FillCircleGradient` wrapper (radial gradient already does exactly this).
- **`Camera2D`/`Camera3D` split into two `Update` tiers: `Update` (no input) and `UpdateWithInput(PrimitiveInput, float/GameTime)` (built-in WASD/mouse controller, reading a caller-owned `PrimitiveInput`).** Previously the plain `Update(float)` *was* the input-reading one — convenient, but meant there was no way to use the camera without its built-in movement (e.g. a fixed camera the game code drives directly, or a controller the game defines itself) without also silently getting WASD/mouse-drag baked in. `Update(float)` was redefined as a truly passive tick (decays shake trauma, settles in-flight `SmoothZoom` easing, touches nothing else); the old input-reading behavior moved to `UpdateWithInput`.
- **`CameraInput`/`CameraInput2D` structs and the `ReadDefaultInput`/`Update(in CameraInput[2D], float)` split were removed** — `UpdateWithInput(PrimitiveInput input, float deltaSeconds)` now reads keyboard/mouse straight from `input` and applies movement inline, no intermediate struct. Rationale: this project's own scope explicitly excludes action-based input mapping (see the scope note below), and the struct-building indirection was solving a problem — decoupling input source from camera logic for replays/networking/tests — this library was never trying to solve; every real caller just wanted "read my `PrimitiveInput`, move the camera." Passing the caller's own `PrimitiveInput` instance in (rather than each camera owning a private one) also means one session's input state drives UI, gameplay and the camera without three separate pollers stepping on each other's delta tracking — `UpdateWithInput` deliberately never calls `PrimitiveInput.Update` itself, only reads it; the caller updates it once per frame and hands the same instance everywhere.
- **`Camera3D`'s right-drag mouse-look and Q/E rebinding.** Mouse-look used to add `MouseDelta` directly to yaw/pitch — mathematically an FPS "steer the direction you push" convention, but this camera's look is gated behind holding a mouse button and dragging, which reads more like "grab the world and drag it" (the convention `Camera2D`'s own left-drag pan already uses, via a `-=`) — confirmed by feel, not just theory, since `Yaw`/`Pitch`'s sign convention was independently verified correct against their own doc comments (positive `Yaw` turns left, positive `Pitch` looks up) via a scratch test before touching anything, ruling out a MonoGame-handedness mixup as the cause. Fixed by negating just the mouse contribution (`yaw -= mouseDelta.X * sensitivity`), leaving arrow-key look untouched since only the mouse drag was reported as backwards. Separately, Q/E moved from roll to yaw (rotate the camera's body left/right, alongside the existing arrow-key yaw) since that's the more commonly reached-for binding next to WASD; the old roll behavior moved to Z/X.
- **`Camera2D` has no parameterless constructor — use `CreateDefault()` instead**, mirroring `Camera3D` (which has never had one, only `CreateDefault()`). A bare `new Camera2D()` was the one asymmetry between the two: harmless as a field-initializer placeholder, but an invisible zero-arg constructor doesn't communicate "this is a placeholder, not a real setup" the way a named factory does.
- **Internal batch helper naming unified across 2D/3D**: `Primitive2DBatch`'s `EnsureStarted`/`_isStarted` renamed to match `Primitive3DBatch`'s `ThrowIfNotBegun`/`_began` (better fit — both classes' public API already calls the paired verb `Begin`/`End`, "started" matched no public method name); `Primitive3DBatch`'s `EnsureSpace`/`EnsureSpaceLine` renamed to match `Primitive2DBatch`'s `Reserve` (fewer call sites to touch, and "reserve" already appeared in `Primitive3DBatch`'s own doc comments). Two separate libraries merged into one assembly early on (see the single-assembly decision above) and these pairs were never reconciled until this pass; grepped for other divergent pairs doing the same job — none found beyond these two and a `_disposed`/`ThrowIfDisposed` guard that only `Primitive3DBatch` has (a behavior gap, not a naming one; left alone here, worth a look if `Primitive2DBatch` ever grows unmanaged resources of its own).
- **Trauma-based screen shake on both cameras** (`AddTrauma`/`Trauma`/`GetShakeOffset`, baked into `GetTransformMatrix`/`GetViewMatrix`). The standard technique (Squirrel Eiserloh's GDC talk, used by Celeste etc.): trauma in [0,1] jumps up on impact and decays linearly, but shake *magnitude* scales with trauma² so the falloff itself feels natural (small bumps barely shake, big hits shake hard) — verified exactly: trauma 1.0 and 0.5 gave offset magnitudes in an exact 4:1 ratio. Sampled from this library's own `Noise` (Perlin), one channel per axis (plus one for rotation/roll), instead of raw random, so consecutive frames drift smoothly instead of jittering. Purely cosmetic — baked into the transform/view matrix only, never touches `Target`/`Position`/`Rotation` themselves, so game logic reading camera state never sees it. 3D applies the offset along the camera's own right/up axes (not world axes) so it reads as camera shake regardless of facing direction. Bug found and fixed while verifying this: the noise-time field defaulted to `0f`, and Perlin noise is exactly 0 at every integer lattice point by construction — so the very first shake read after `AddTrauma` (before any `Update` tick) was silently zero; fixed by seeding it to a non-integer value instead.
- **Blend state defaults to `NonPremultiplied`, not `AlphaBlend`.** Every color in this codebase is straight (non-premultiplied) RGBA. `AlphaBlend` expects premultiplied input — feeding it straight color renders translucency far too opaque (verified: alpha 0.15 rendered as ~0.82). If you ever need `AlphaBlend` for a specific draw, premultiply the color yourself first.
- **3D face winding sets the light/cull normal** (`Cross(b-a, d-a)` in `PushQuadLit`/`PushTriangleLit`). Wrong winding is invisible under the default (unlit, `CullNone`) but breaks lighting/culling. Verify winding explicitly for any new 3D filled shape — a flat unlit render proves nothing.
- **Sphere/hemisphere pole quads use an exact outward normal (`point - center`), not the edge cross-product `PushQuadLit` derives by default.** At a pole ring two of the quad's four corners coincide (`FillSphereEx`'s `ry0`/`ry1` or `FillHemisphere`'s `ringRadius`), so the cross-product normal goes zero-length; `Vector3.Normalize` turns that into NaN, and `Shade` casting NaN to a byte silently blackens the whole quad — the reported "top of the sphere doesn't light up" bug. Fixed via a `PushQuadLit` overload taking an explicit normal.
- **No `Ex`/`V`-suffixed method siblings — every parameter variant is an overload of the same name.** `FillSphereEx`/`FillCylinderEx`/`FillCubeV`/etc. were folded into `FillSphere`/`FillCylinder`/`FillCube` overloads (distinguishable by argument types/count, so no ambiguity). `Fill`/`Border`/`Draw` per shape is the naming surface — a different parameter list is a new overload, not a new name.
- **`FillTriangleGradientRounded`/`DrawTriangleGradientRounded` color each rounded-boundary point by exact barycentric weight against the true triangle, not by nearest-original-vertex.** A rounded triangle gradient is still just 3 fan triangles sharing the original `boundary[0]`-ish apex; coloring by "nearest vertex" (flat per-corner color, `FillPolygonGradientByNearestVertex`'s approach) makes each thin fan wedge blend two *nearly-identical* colors against one *different* one, which shows as visible radial creases converging on the apex — confirmed by rendering and sampling a scanline (0 monotonicity reversals after the fix, versus visible banding before). Barycentric coloring reproduces the exact same continuous gradient `FillTriangle`'s single GPU-interpolated triangle has, because barycentric weights are affine and agree across any triangulation of the same triangle. Barycentric coordinates aren't well-defined for a general N-gon, so `FillPolygonGradient`/`FillPolygonGradientRounded` use a different, N-gon-appropriate fix instead — see the next entry.
- **`FillPolygonGradient`/`FillPolygonGradientRounded` color every vertex by its own distance from `points[0]` (normalized against the farthest point), not a binary "`points[0]` gets `from`, every other vertex gets `to`".** The binary scheme meant only the few triangles actually touching `points[0]` in the fan showed any blend at all — every other wedge was flat-`to`-colored — which reads as visible facet seams on a many-sided shape (confirmed by rendering a 5-point star: creases cutting across it). `ComputeDistanceGradientColors` gives every vertex a distinct, smoothly-varying color instead, cutting adjacent-sample color jumps on an inner ring from constant creasing down to 1/179 (residual, not a seam). Both methods also gained the same `IsConvex`/`TriangulateEarClipping` split `FillPolygon` uses (including inside `FillPolygonGradientByNearestVertex`'s own triangulation, for the *Rounded* variant) — a concave shape's fan-from-`points[0]`/`boundary[0]` has the same wrong-topology risk regardless of whether the fill is a solid color or a gradient.
- **A `Border*`'s round/bevel `jointRadius` is capped by its own thickness on a sharp corner — not a bug, a self-intersection guard.** For an inward-only stroke, `ComputeJoint`'s fillet can't reach further than roughly `thickness × cos(halfAngle)` before it would cross the shape's own opposite edge; requesting a much larger radius on a thin border and a sharp point (e.g. a 5-point star's ~42° tips) silently clamps down to a few pixels — correct, but easy to mistake for "the join isn't working" in a screenshot. Verified by reproducing `ComputeJoint`'s math standalone and by rendering `DrawLineStrip` (a symmetric, non-inward-only stroke) with the same three join types: Miter/Round/Bevel produced clearly different, correct results (a 7px/25px/33px difference in how far a sharp corner's spike reached) once thickness was large enough for the difference to be more than a couple of pixels.
- **`FillPolygon` now ear-clips instead of always fan-triangulating.** The fan-from-`points[0]` needed vertex order to sweep monotonically in angle from `points[0]` — a plain 5-point star's outer tip CAN see the whole star (a real "star-shaped from that vertex" kernel point) but its alternating outer/inner vertex order still doubles back angularly as seen from that tip, so the naive fan produced overlapping-and-gapping triangles (confirmed by rendering: a torn chevron, not a star) — visibility alone wasn't the right test. `IsConvex` (O(n), one pass of consecutive-turn cross products) now gates which path runs: convex or triangle input keeps the original zero-allocation fan (no behavior/perf change for the common case — rounded-corner boundaries, regular polygons, etc.); anything else goes through `TriangulateEarClipping` (standard O(n²) ear-clipping over a `prev`/`next` index list, winding-aware convex/reflex test, `PointInTriangle` guard against a remaining vertex poking into a candidate ear). Verified by rendering a 5-point and an 8-point star and probing tips (filled) vs valleys (empty at outer radius, filled at inner radius) — both correct — plus a convex-hexagon regression check confirming the fast path's pixel count is unchanged. Still requires a *simple* (non-self-intersecting) polygon; that case falls back to the old fan rather than drawing nothing.
- **`BorderRing`'s two arcs are drawn as one joined `DrawLineStrip` (`LineJoin.Round`), not one `DrawLine` per segment.** Independent per-segment lines each get their own flat, perpendicular end cap; those caps don't line up on a curve, and once `thickness` is more than a pixel or two the ring reads as a sawtooth of small tick marks instead of a smooth arc (confirmed by rendering: the joined-strip version has zero radial spikes past the expected outer radius, checked at every degree around the ring; the old per-segment version did not). A partial ring's wedge (arc + 2 radial closing edges) is one single closed `BuildOutlineGeometry` loop, not 2 open strips plus 2 separate `DrawLine` calls — 4 independently-rasterized primitives that only *touch* at a shared edge don't reliably rasterize with zero gap even when the underlying math agrees exactly (confirmed by scanning the rendered wedge's boundary for stray transitions: 8 before, 2 — start and end of the one real gap — after). Same lesson applied to `DrawCircleSectorLines`'s two radial edges: they used to be 2 independent `DrawLine` calls sharing only their start point at `center`, leaving an uncovered wedge at that apex (each line's own flat end cap is oriented to its own direction, so the two don't tile) — now one `DrawLineStrip([a, center, c], ...)` with a real joint there.
- **`BuildRoundedOutlineGeometry`'s segment quads must pick `TangentIn`/`TangentOut` vs `InnerCorner` based on that joint's own `Side`, not always "outer=Tangent*, inner=InnerCorner".** A joint's `Side` flips per-vertex with the turn direction (e.g. opposite at a "peak" vs the following "valley" of a zigzag) — when `Side` is negative, `TangentIn`/`TangentOut` actually land on the physical side the segment loop's non-joint fallback (`sideA`/`sideB = 1`) assumes is *inner*. Using them unconditionally as "outer" produced a self-intersecting (bowtie) segment quad — a real gap/crack cutting across the stroke on **any open polyline whose turn direction alternates** (a zigzag/wave being the simplest case) with `LineJoin.Round`/`Bevel`. Confirmed two ways: rendering an open zigzag `DrawLineStrip` showed the crack and a perpendicular scan across the cracked segment found a real background gap between two solid runs (not a single run); a standalone reproduction of the exact quad corners gave a shoelace signed area of exactly 0 (the classic self-intersecting-quad signature) before the fix, and a clean non-crossing quad after. Fixed by swapping which of `TangentIn`/`TangentOut`/`InnerCorner` plays "outer" vs "inner" per joint based on that joint's `Side` sign. Closed shapes (`BorderPolygon`/`BorderTriangle`/etc.) go through the same code but weren't hit by this in prior testing — re-verified after the fix via pixel-count sanity checks on an L-shape and a star border, no regression.
- **`ComputeJoint`'s rounded-corner clamp is per-corner, not per-edge** — two adjacent corners can together claim over 100% of a shared edge before either individually hits its own 90% cap. `ClampCornerRadiusToFit` pre-clamps the requested radius before `ComputeJoint` runs, rather than changing `ComputeJoint`'s shared constant (used by many already-verified callers).
- **Never name "raylib" (or other design inspirations) in doc comments or user-facing docs.** Standing rule — see CODESTYLE.md.
- **`GridRenderer2D` (texture-backed grid/heatmap renderer) removed — out of scope.** Built once (measured ~1000x faster than per-cell `FillRectangle` past ~1% grid visibility) but it's a ready-made *system* (owns a `Texture2D`, a `SetCell`/`Fill`/`Apply`/`Draw` lifecycle), not a primitive/building block — this library hands out the pieces to build a grid/heatmap yourself (`FillRectangle` in a loop, or your own texture), not a finished renderer for one. Same reasoning that kept a particle system and a cellular-automata helper out: constructing the higher-level thing yourself is the point of reaching for this library in the first place.
- **`tests/MonoPrimitives.Tests/` is a headless `Game`, not an xUnit project.** `Primitive2DBatch`/`Primitive3DBatch` both need a real `GraphicsDevice` (`BasicEffect`, `DrawUserPrimitives`), and MonoGame only creates one inside a running `Game` — a standard test runner never gets one. So the pure-math checks (`Collision2DTests`/`Collision3DTests`/`NoiseTests`/`ColorUtilTests` — no device needed) and the GPU-dependent ones (`ShapeTests3D`, `LightingRegressionTests` — one call per public 3D shape method, asserting it actually emitted triangles/lines without throwing) all run once from `TestRunnerGame.Draw()`, print a pass/fail summary, and exit with the process code a CI step would check (0/1). Two of `Noise`/`Collision3D`'s regression tests directly encode bugs found and fixed earlier this project (`Sample1D`'s degenerate near-zero rate, `GetRayCollisionBox`'s scale-dependent normal) so they can't silently regress. Running the completed suite immediately caught one real issue — not in the library, but in the test itself: `DrawPoint3D` was asserted to emit triangles, but it's implemented via `DrawLine3DFast` and emits lines.
- **3D line-stroke methods (`DrawLine3D`, `DrawArrow`, `DrawLine3DDashed`, `DrawSplineCatmullRom3D`, `DrawSplineBezierCubic3D`) take `thickness`, not `width`; `DefaultLineWidth` is `DefaultLineThickness`.** CODESTYLE.md already documents `thickness` as the canonical name ("`thickness` defaults to `1f` on `Border*`/`Draw*`"), and every `Border*`/`Draw*` method in this same file already used it — only the line-drawing family had drifted to `width`, inconsistent with its own neighbors, not a deliberate 2D/3D split. `FillCube`/`BorderCube`/`DrawCube`'s `width` parameters are untouched — those are genuine box dimensions, a different concept entirely. No external callers used the old name (checked before renaming; the library isn't published yet, so this is a good time for it).
- **2D's two `DrawArrow` overloads collapsed into one** (`color` before `thickness`, both with defaults, plus optional `headLength`/`headWidth`) instead of `(start,end,color,thickness=2f)` and `(start,end,thickness,color,headLength,headWidth)` as separate overloads with `color`/`thickness` in a different order from each other. C# requires required parameters before optional ones, and `color` has no sensible default, so `color` before `thickness` was already forced on the simple overload — the detailed overload just hadn't followed suit.
- **`Camera3D.GetScreenToWorld`/`GetWorldToScreen` renamed to `ScreenToWorld`/`WorldToScreen`**, dropping the `Get` prefix to match `Camera2D`'s names for the exact same operation. `GetScreenToWorldRay` keeps its prefix — unlike a plain position conversion, "get a ray" reads naturally as fetching a distinct object. `Camera3D.ClampToBounds` also made public to match `Camera2D.ClampToBounds` (a 3D caller setting `Position` directly had no way to re-apply the clamp).
- **`Plot2D` was removed.** A dedicated charting library (ScottPlot, etc.) will always render better axes/legends/data-scale handling than a primitive-drawing library's bolt-on chart type — not worth maintaining. This library draws shapes; plotting real data belongs elsewhere.
- **Every `examples/` project needs its own `app.manifest` with `<ApplicationManifest>app.manifest</ApplicationManifest>` in the `.csproj`, copied from `samples/MonoPrimitives.Sample/app.manifest`.** Without a manifest declaring `dpiAware`/`dpiAwareness`, Windows treats the process as DPI-unaware: on any display scaled above 100%, the OS renders the window at its logical size and bitmap-stretches the output to fill the physical pixels, softening *everything* uniformly — including `DebugFont5x7`'s pixel-art glyphs — regardless of what the app actually draws. All 13 `examples/test`/`examples/demos` projects were missing this (an oversight when each was scaffolded — only the pre-existing sample had it), reported as "every test looks blurry."
- **`examples/demos/` stopped at 6 games + 1 simulation: Breakout, Tetris, Asteroids2D, Asteroids3D (the required 5.1-5.4), Platformer2D (explicitly requested, to exercise 2D collision/camera-follow/shake together), Snake and Boids (2 original additions — "up to 2 more, stop early if fewer suffice"; Boids specifically to demonstrate the "simulations" half of this project's scope, since every other demo is a game).**
- **`Asteroids3D`'s flight model rotates yaw/pitch around world Up, not the ship's own (drifting) up vector; roll (Q/E) is purely a cosmetic camera bank, never fed back into flight.** The first version rotated yaw around `_shipUp` and let pitch tilt `_shipUp` too (standard rigid-body Euler integration) — reported as "controls feel super weird"; reproduced by combining sustained pitch then yaw and checking whether yaw's rotation axis had drifted (verified: it had — after pitching, a "yaw" no longer turned cleanly left/right, since it was now rotating around a tilted axis). Recomputing `_shipUp` fresh every frame from `_shipForward`/world `Vector3.Up` (never accumulating roll into it) makes yaw/pitch behave predictably regardless of the ship's current attitude — verified via a scratch test: a pure yaw applied while pitched changed `Forward`'s vertical component by exactly 0.0000, versus a real drift under the old scheme. Pitch is clamped to 5° short of straight up/down to avoid `Cross(Forward, Up)` degenerating there. This trades true 6DOF tumbling for a much more controllable arcade feel — the right tradeoff for a demo, not a flight sim. **Left/Right were inverted** — `RotateShip` rotated `_shipForward` around `+Vector3.Up` by the raw `yaw` value (`GetAxis(Keys.Left, Keys.Right)`, positive on Right), but rotating around `+Up` moves a vector toward `Up × Forward`, which is `-(Forward × Up)` — the ship's own `-Right` (its own right is defined as `Cross(Forward, Up)`, same convention `RotateShip`'s pitch/camera code already uses). So holding Right turned the nose left. Confirmed via a standalone `System.Numerics` scratch reproduction (not the live game — GUI keyboard automation proved unreliable in-session): the old code's `(newForward - forward)·right = -0.0267`, the fix's (negate the rotation angle: `Matrix.CreateFromAxisAngle(Vector3.Up, -yaw)`) `= +0.0267`.
- **`Platformer2D`'s ground collision uses a float overlap check, not `Collision2D.CheckCollisionRecs(PlayerRect(), plat)`.** `PlayerRect()` truncates the player's float position into an `int Rectangle`; since `PlayerHalfHeight*2` has no fractional part, that truncated rect stays byte-for-byte identical across almost a full pixel of vertical movement. Combined with `Rectangle.Intersects`' strict (non-inclusive) comparison, resting exactly on a platform after landing needed nearly a full pixel of gravity-driven interpenetration before the truncated rect re-registered as touching — confirmed by simulation: `_grounded` cycled false/false/true while standing perfectly still, true only 1 frame in 3, not every frame. Jump only fires on the single-frame `IsKeyPressed` edge, so roughly two-thirds of jump presses landed on a "not grounded" frame and were silently dropped — reported as "jump randomly doesn't work." Switched the movement-resolution overlap check (not the stomp/goal checks, which aren't gated behind a single input edge and don't need it) to a direct float comparison against the platform `Rectangle`'s edges; verified `_grounded` is now `true` on every frame at rest, with a clean jump-up/fall-back-down cycle landing at the exact same Y each time.
- **`Boids`' neighbor search uses a wrap-aware toroidal distance, not a plain position subtraction.** The demo wraps boid positions at the screen edges (same as `Asteroids2D`), but a plain `self.Position - other.Position` treats a boid just past the right edge and one just past the left edge as far apart even though they're visually adjacent after wrapping — confirmed by rendering: the flock piled into two dense, nearly stationary clumps sitting exactly on the wrap corners before the fix, scattered naturally into several moving clusters after switching to a `ToroidalDelta` that picks the shorter path around each axis.
- **`BoxingViewportAdapter2D`/`ScalingViewportAdapter2D.Scale`/`Offset` read `Device.PresentationParameters.BackBufferWidth/Height`, not `Device.Viewport.Width/Height`.** `Apply()` narrows `Device.Viewport` to the letterboxed rect; reading `Scale`/`Offset` again afterward (e.g. from `Camera2D.ScreenToWorld`/`WorldToScreen` later the same frame) recomputed from that already-narrowed size and silently returned a wrong `Offset`. Found via a `ScreenToWorld(WorldToScreen(x))` round-trip test — 30+ px drift before, 0 after. **Separately: never call `Apply()` before drawing with `camera.GetTransformMatrix() * adapter.GetScaleMatrix()`.** `GetScaleMatrix()` already bakes in `Offset`; narrowing the device viewport too physically re-offsets the same amount again on top, so content lands correctly on X but drifts on Y by roughly `Offset.Y` (only visible with a nonzero `Offset`, i.e. `BoxingViewportAdapter2D` — confirmed via a rendered-pixel-centroid check, not just the round-trip math, since the math alone still round-trips consistently even under the double-offset). Both the sample and `ViewportTest` called `Apply()` this way; fixed by dropping the `Apply()` call from the 2D draw path (bars show correctly from just not drawing there, given a full-window `Clear` first) — `Apply()` is still correct as `Primitive3DBatch.Begin(camera)` uses it internally (scale-only projection, no separate `Offset` add). Keeping `Apply()` (device-viewport narrowing, for 3D) and `GetScaleMatrix()` (matrix composition, for 2D) as two separate mechanisms — rather than one `Apply()`-only path — is deliberate: it's what lets one `ViewportAdapter2D` instance drive a 2D layer and a 3D layer sharing the same window in the same frame. MonoGame.Extended's own adapter never had to solve this, since it has no 3D story at all.
- **`RandomUtil` added to Core: seedable float-based distribution sampling.** Requested specifically for the simulation use cases `PROJECT.md` already names (predator-prey, pandemic models, cellular automata) — none of `Uniform`/`Bool`/`Gaussian`/`Exponential`/`Poisson`/`Binomial`/`LogNormal`/circle-and-sphere sampling existed before this. Design choices worth knowing: **Gaussian uses Marsaglia polar, not Box-Muller** — avoids any `Sin`/`Cos` call, consistent with this codebase's existing "no trig on a hot path" stance (`UnitCircleLut`/`TrigLut`); its rejection loop always yields a *pair* of independent samples, so the second is cached and returned on the next call instead of discarded. **Poisson/Binomial switch to a Gaussian (or, for large-trials-tiny-probability Binomial, Poisson) approximation above a lambda/variance threshold** rather than simulating directly — both are otherwise O(lambda)/O(trials) per sample, an unbounded cost this library avoids elsewhere too (`Primitive2DBatch.SegmentsForArc`'s capped segment count). **Circle/sphere sampling uses plain `MathF.Sin`/`Cos`, deliberately not `UnitCircleLut`/`TrigLut`** — those live in the 2D/3D namespaces, and Core is the shared foundation 2D/3D depend ON, so reaching back into either would invert that dependency; the cost profile also differs (one trig call per sample here, not per vertex of a many-triangle shape). All of the above verified empirically (200k-sample runs matching theoretical mean/variance per distribution, including both approximation regimes) before being trusted — same house rule as everything else, extended here to statistical rather than geometric correctness.
- **`RandomUtil.Shared`: a static, thread-safe mirror of the instance API, built on `Random.Shared`.** The instance API is deliberately not thread-safe (one `Random` stream per instance, same single-threaded assumption `Noise`/`PrimitiveInput`/the cameras already make) — parallelizing a large agent-based sim should give each thread its own seeded instance rather than share one, which is cheaper than locking and just as reproducible (streams across threads can't be deterministically interleaved anyway, seeded or not). `Shared` exists for the case where reproducibility isn't wanted but thread-safety is: it has no seed constructor since `Random.Shared` has no single reproducible sequence once more than one thread touches it. Its Gaussian spare-value cache is `[ThreadStatic]` (named `_threadSpareGaussian`, distinct from the instance field `_spareGaussian` — same name in both would read as one shared cache) rather than locked, mirroring how `Random.Shared` itself stays thread-safe internally. Verified with a 16-thread stress test hammering every method concurrently: zero exceptions, correct aggregate mean/variance.
- **`DrawSplineBasis`/`DrawSplineBezierQuadratic` added, matching raylib's spline set.** `DrawSplineBasis` (uniform cubic B-spline) is an *approximating* spline, unlike `DrawSplineCatmullRom` — it does not pass through any of its control points, only staying inside their shape; verified by rendering both on the same 4 points side by side (Catmull-Rom visibly touches the points, Basis visibly doesn't). Both new methods expose `join`/`cap`/`jointRadius` (same shape as `DrawSplineLinear`), unlike the existing `DrawSplineCatmullRom`/`DrawSplineBezierCubic` (Miter-only) — deliberate for the two new ones, existing two left untouched since retrofitting wasn't asked for; verified the `join`/`cap` params actually take effect by rendering a 2-piece `DrawSplineBezierQuadratic` with `LineJoin.Round` and confirming the shared joint renders as a smooth curve, not a sharp corner.
- **`Collision2D` polygon/mixed-shape checks added** (`CheckCollisionPolyPoly`/`RecPoly`/`RecTriangle`/`TriangleTriangle` via a shared SAT core; `CheckCollisionCirclePoly`/`CircleTriangle` via a separate closest-point-on-edge approach). The two groups are NOT interchangeable: SAT requires convex input and gives a wrong answer silently on a concave one, so the circle checks deliberately don't use it — verified directly with a concave "arrow" polygon: a circle placed in its concave notch (inside the bounding shape but outside the true polygon) correctly reports no collision.
- **Two new spline methods, `DrawSplineBasis` (uniform cubic B-spline) and `DrawSplineBezierQuadratic`, expose `join`/`cap`/`jointRadius`** — unlike the existing `DrawSplineCatmullRom`/`DrawSplineBezierCubic`, which only ever use the Miter default and don't expose those params at all. Deliberate for the two new ones only; the existing two were left untouched since retrofitting wasn't asked for, so the four spline-drawing methods aren't fully symmetric today. `DrawSplineBasis`'s defining property worth remembering: unlike every other spline here, it does NOT pass through its own control points (an approximating, not interpolating, spline) — verified by rendering it against `DrawSplineCatmullRom` on the identical 4 points side by side.
- **2D `Capsule` (`FillCapsule`/`BorderCapsule`/`DrawCapsule`/`FillCapsuleGradient`/`DrawCapsuleGradient`) added, each with BOTH an endpoint-pair overload and a `center, length, radius, ..., rotation` overload.** The endpoint-pair form came first (it's what the geometry is actually built on and stays more convenient for chain links/ropes/limbs); `rotation` was added right after specifically because every *other* shape in this file follows a "center + size(s) + rotation" convention and this one didn't — the two forms aren't redundant, they're kept side by side, with the center-based ones just computing endpoints (`CapsuleEndpointsFromCenter`) and forwarding. `FillCapsule` itself is exactly the geometry `DrawLine(..., LineCap.Round)` already draws (a straight quad plus two round end caps) — reused directly via the existing private `EmitLineSegment`/`EmitRoundCap` rather than duplicated. `FillCapsuleGradient`'s "distance from center" is distance from the axis SEGMENT, not a point: each outer boundary vertex pairs with whichever pole (`start` or `end`) its own cap arc surrounds, and GPU interpolation across the straight-side quads then exactly reproduces the true closest-point-on-segment gradient there — not an approximation of it, verified by rendering, along with the rotation math (an analytically-computed expected endpoint landed exactly on the rendered 30°-rotated capsule's edge). `start == end` (or a zero-length center form) degenerates to the matching `*Circle`/`*CircleGradient` method rather than producing degenerate geometry.
- **Gallery2D's `GradientFrom`/`GradientTo` changed from `Turquoise`/`Wisteria` (teal-to-purple) to `PeterRiver`/`Alizarin` (blue-to-red).** User feedback: disliked the original combo on sight ("esta feo"). Single constants used by nearly every shape's gradient cells, so the fix is one line, not a per-cell change. Picked deliberately distinct from the existing `GradientFrom2`/`GradientTo2` warm pair (`Sunflower`/`Pomegranate`) so the gallery's two gradient "looks" stay visually distinct from each other.
- **`ColorUtil` blend modes added: `Multiply`/`Screen`/`Overlay`/`Additive`.** Pure `Color x Color -> Color` functions for computing a blended color value to draw normally afterward — not a GPU blend-state operation (`Primitive2DBatch` already uses one `NonPremultiplied` state throughout). Alpha is always taken from the first argument unchanged in every mode (these blend color, not transparency) — verified explicitly, along with each mode's defining identity (`Multiply(white, x) == x`, `Screen(black, x) == x`, `Additive` clamping at 255 instead of overflowing).
- **Shadows, Poly-rounded, DrawPolygonGradientRounded, and Ellipse linear gradient added — all "fill in the missing sibling variant a peer shape already has," found by auditing the full Fill/Border/Draw method matrix rather than guessing.** `FillTriangleShadow`/`FillEllipseShadow`/`FillPolyShadow`/`FillPolygonShadow`/`FillCapsuleShadow` all reuse the existing `FillOutsetGradientRing` halo machinery (only Circle/Rectangle had shadows before). `FillPolygonShadow` shares `InsetConvexPolygon`'s sibling `OutsetConvexPolygon` reflex-vertex limitation (see ROADMAP.md) — confirmed by rendering the same L-shape gallery cell, same distorted corner. `FillPolyRounded`/`BorderPolyRounded`/`DrawPolyRounded` give regular n-gons the same standalone `*Rounded` naming Triangle/Rectangle/Polygon already have (the capability already existed via `BorderPoly`/`DrawPoly`'s `join`+`jointRadius`, just without the discoverable name). `FillEllipseGradientLinear`'s `rotation` deliberately does double duty — tilts the ellipse's own H/V axes AND the gradient's reading axis together, unlike `FillCircleGradientLinear` (where only the axis rotates, since a circle has no shape orientation of its own to lock it to) — verified by rendering a 90°-rotated horizontal gradient against the plain vertical one and confirming they match. All of the above verified by rendering, not just reasoning, including the capsule shadow's degenerate (`start == end`) fallback to a circle shadow.
- **`Collision2D`: added `CheckCollisionCircleCapsule`/`CheckCollisionCapsuleCapsule`, and `DrawCircleSectorGradient`/`DrawRingGradient` added to `Primitives2D.cs` — both found by auditing actual coverage after adding Capsule, not by guessing.** Capsule had zero collision support despite being the shape most commonly reached for specifically because of cheap, robust collision math; `CapsuleCapsule` uses a private `SegmentSegmentDistanceSquared` (Ericson's closest-point-between-segments algorithm, handles every degenerate case — both-points, one-point, parallel — without explicit branching on them) rather than anything approximate. Verified against 10 hand-computed cases (parallel separated/overlapping, perpendicular crossing, T-junction clamping on both sides of the radius-sum threshold, exact end-to-end boundary distances, and a degenerate-point capsule cross-checked against `CheckCollisionCircleCapsule` for an exact match) before trusting it. Sector/Ring were the one shape family with no gradient variant at all (every other shape has one) — `DrawCircleSectorGradient`/`DrawRingGradient` fill that gap, keeping the pair's existing `Draw`-not-`Fill` naming quirk (they're fill-only, no separate border pass, but were named `Draw*` from the start) rather than introducing a new naming inconsistency to "fix" it.
- **`FillPolyGradientRounded` and Sector/Ring shadows (`DrawCircleSectorShadow`/`DrawRingShadow`) added — the last two gaps found by the same matrix-audit method.** `FillPolyGradientRounded` deliberately drops `innerOffset`/`outerOffset` (unlike the base `FillPolyGradient`), matching `FillTriangleGradientRounded`'s own simpler rounded-corner variant rather than inventing new complexity. Sector/Ring shadows special-case a full-turn sweep (skip the center-point "spike" a wedge shadow would otherwise need, since a full-turn sector/ring has no radial cut edges to shadow) and — per `FillCircleShadow`'s own precedent — a full ring's shadow only glows on its outer edge, not around the inner hole. Verified by rendering all of the above, including the full-turn special case (confirmed no spike artifact) and `FillPolyGradientRounded` at `cornerRadius=0` matching the sharp-corner version exactly.
- **Gallery2D's gradient cells now cycle through `Palette.GradientPairs` per shape row instead of two fixed pairs everywhere.** User feedback: the previous fixed pair (`PeterRiver`→`Alizarin`, a cross-hue blue-to-red) looked bad — flat colors picked for two different roles don't necessarily make a good gradient together. `Palette.GradientPairs` is specifically curated for this (each pair is a light/dark version of one hue), so a small `GradientPair(index)` helper now picks a pair by index (wrapping via modulo), and each shape's `Cells()` method locally shadows the (removed) `GradientFrom`/`GradientTo`/`GradientFrom2`/`GradientTo2` names with its own row-specific pair — every existing cell body needed zero changes, since C# resolves the same names to the new local shadow automatically.
- **`DrawPolyGradientRounded` added (last gap in the Poly matrix) — first draft was subtly wrong, caught before committing.** The first version built the fill as an independently-clamped SMALLER regular polygon (radius minus thickness) reusing the corner radius already clamped safe for the full-size (border) polygon — but a corner radius safe for a polygon's edges isn't necessarily safe for a uniformly smaller copy of the same polygon, since its edges are shorter too. Rewritten to match `DrawPolygonGradient`'s existing Round-join approach instead: round the true outer polygon once (`BuildRoundedCornerBoundary`), then inset that already-rounded boundary inward by `thickness` (`InsetConvexPolygon`) rather than re-deriving and re-clamping a second polygon from scratch. Verified by rendering the exact stress case that would have broken the first version (large corner radius + thick border, relative to radius, on low-side-count polygons) — renders clean with no self-intersection.
- **`DrawRingShadow`'s partial-wedge case computes its offset boundary directly in closed form (grow outer radius, shrink inner radius, same angle range) instead of through the shared `OutsetConvexPolygon`-based halo helper every other `Fill*Shadow` uses.** A ring wedge's inner-arc corners are genuinely concave (reflex) from the outward-growth perspective — exactly the case that helper's centroid-heuristic offsetter gets wrong (same root cause as `InsetConvexPolygon`'s documented reflex-vertex limitation). Went through two iterations, both caught by rendering: first version routed through the shared helper — produced a lopsided, blobby halo. Second version fixed the shape but widened the angular sweep slightly at each tip to flare the two straight edges outward — that made the true and offset boundaries sweep different total angles across the same segment count, so their per-index correspondence drifted, producing a spike at each tip. Final version keeps both boundaries at the exact same angles (only radius differs) — no shear, at the cost of the two tip corners not getting their own rounded glow (the shadow just stops at the wedge's own angular edge there) — a deliberately simpler, artifact-free trade over a fully mitered corner.
- **`FillTriangleShadowRounded`/`FillPolyShadowRounded`/`FillPolygonShadowRounded` added — Rectangle's shadow already took a `RectCorners radius` (rounding built into its one shadow method via `RectCorners`' float-conversion convenience), but Triangle/Poly/Polygon's shadows only ever drew the sharp-corner shape.** Mirrors each shape's existing sharp-vs-`*Rounded` split (matching `FillTriangleRounded`/`FillPolyRounded`/`FillPolygonRounded`) rather than Rectangle's single-method approach, since Triangle/Poly/Polygon don't have an equivalent single type unifying "no rounding" and "rounding by X". `FillPolygonShadowRounded` inherits `FillPolygonShadow`'s reflex-vertex caveat unchanged on a concave input — rounding a reflex corner leaves it concave, just an arc instead of a sharp point, confirmed by rendering the L-shape gallery cell (same small artifact at the notch, not a new one).
- **`GridRenderer2D` removed — out of scope.** See the "Real gaps" note above; it was a ready-made system (owns a `Texture2D`, its own `SetCell`/`Fill`/`Apply`/`Draw` lifecycle), not a primitive/building block. Same reasoning that already kept a particle system and a cellular-automata helper out of this library.
- **Rectangle's `*RoundedGradient`/`*ChamferGradient` renamed to `*GradientRounded`/`*GradientChamfer`; `GetSplinePointBezierQuad` renamed to `GetSplinePointBezierQuadratic`.** A final naming audit found Rectangle was the one shape putting its corner-modifier before "Gradient" while Triangle/Poly/Polygon all put "Gradient" first (`FillTriangleGradientRounded`, etc.) — Rectangle's four `Fill*`/`Draw*` pairs were renamed to match rather than documented as a permanent exception. The spline helper's "Quad" was likewise brought in line with the "Quadratic" already used by `DrawSplineBezierQuadratic`, which calls it. Both are breaking renames, made deliberately rather than kept for compatibility: the package is on NuGet but still pre-1.0 (0.2.x), where semver doesn't promise API stability yet — this is the cheapest point to fix a naming mistake. Every call site (library, sample gallery, examples) updated in the same pass and confirmed by grep — none missed.
- **`Camera2D`/`ViewportAdapter2D` audit found and fixed 4 issues.** (1) `UpdateWithInput`'s left-mouse-drag pan never compensated for `Rotation`, breaking its own documented "drag tracks the cursor 1:1" contract whenever the camera is rotated — verified numerically before and after (a 90°-rotated, 2x-zoomed camera's 30px screen drag drifted by `(-30,+30)` with the old code, exactly `(0,0)` once `mouseDelta` is rotated by `-Rotation` before dividing by zoom). Dormant until now: no sample/demo currently rotates a 2D camera. (2) `Camera2D` had no `Reset()`, unlike `Camera3D`'s exact analog (restores construction-time pose, clears follow/zoom/shake state, bound to `R`) — added the same shape (`_initialTarget`/`Offset`/`Rotation`/`Zoom` stored at construction), verified by mutating everything then round-tripping through `Reset()`. (3) `ViewportAdapter2D.BoundingRectangle` mixed truncation (`(int)offset.X/Y`) with rounding (`MathF.Round` for width/height) — could land up to 1px off from the float-precise `GetScaleMatrix()` draw transform when `Apply()` is used alone. Normalized to `MathF.Round` for both. (4) The `Camera2D(ViewportAdapter2D, ...)` constructor snapshotted `Offset` once from `VirtualWidth/VirtualHeight` at construction — harmless for `Boxing`/`ScalingViewportAdapter2D` (fixed virtual size for their whole lifetime) but silently stale for `Default`/`WindowViewportAdapter2D` (whose virtual size deliberately tracks the live device/window size) after any resize, since `GetCameraMatrix()` folds that frozen `Offset` into every downstream method. Decompiled MonoGame.Extended's actual `OrthographicCamera` to check its approach first: `Origin` there is *also* just a construction-time snapshot (confirmed directly against its source) — Extended doesn't solve this either, and its real reactive mechanism (`GameWindow.ClientSizeChanged`, used by `BoxingViewportAdapter` to keep `GraphicsDevice.Viewport` in sync) targets a different problem this library already solves differently (`Scale`/`Offset` computed live from `PresentationParameters` on every read, no event/cache — see the adapter family's own row above). So `Offset` now follows that same "live, no cache" philosophy instead: it re-reads the adapter's virtual center on every access until assigned directly even once, which pins it (matching old field behavior exactly from then on); `Reset()` un-pins it again. Verified: constructed against a fake live-tracking adapter, confirmed `Offset` followed 3 simulated resizes correctly, pinned after an explicit assignment despite a further resize, and resumed live-tracking after `Reset()`.
- **`Guide/Primitive2DBatch_Guide.md` added, migrating and superseding `Design/2D/Primitives2D_Guide.md` (deleted).** Same "one file per topic in `Guide/`" pattern as `RandomUtil_Guide.md`: every current method grouped by shape family (including everything added this session — Capsule, all shadows, Poly-rounded, Sector/Ring gradients), plus a dedicated section on how shadows are built (`OutsetConvexPolygon`, the shared reflex-vertex limitation, linking `ROADMAP.md`) and the gradient-inset composition rule. The old `Design/2D/` copy predated this session's additions and would have been stale immediately; keeping both would just invite drift between two descriptions of the same API.
- **`Camera3D.EyeHeight`'s doc comment rewritten — it was never actually applied anywhere (confirmed by grep: unused outside its own declaration), despite claiming to be "applied in first person mode."** Reviewed `UpdateWithInput`'s `FirstPerson` branch: head-bobbing only ever nudges the *existing* `Position.Y`/`Target.Y` by a small delta, never resets to a base height — meaning the design already assumes the caller sets `Position.Y` to `groundY + EyeHeight` themselves each frame (only the game knows ground/terrain height; auto-applying it would make the camera a ground-tracking physics system, the same reasoning that ruled out `GridRenderer2D`). Left the numeric default alone, fixed the comment to describe it as a suggested reference value the caller applies, not automatic behavior. No sample/demo exercises `CameraMode.FirstPerson`, so this was dormant.
- **`Guide/Camera2D_Guide.md` added, migrating and superseding `Design/2D/ViewportAdapter_Guide.md` (deleted).** Covers `Camera2D` in full (nothing had a dedicated guide before — ARCHITECTURE.md only had one summary line) plus the 4 `ViewportAdapter2D` types, folded into one guide since a `Camera2D` is normally constructed *with* an adapter. Includes the `Offset` live-tracking behavior and its direct comparison against MonoGame.Extended's actual `OrthographicCamera.Origin` (decompiled to check), and the `Apply()`-vs-`GetScaleMatrix()` footgun with the exact 2D+3D-coexisting pattern from `ViewportTest`.
- **`FontGlyphs5x7`: `a`/`e`/`g`/`u` (and their accented `á`/`é`) were one row taller than every other x-height lowercase letter — fixed.** Every non-ascender lowercase glyph is supposed to occupy exactly 5 rows (rows 2-6, leaving rows 0-1 blank) to sit on a shared x-height baseline — confirmed by tabulating every lowercase glyph's first/last non-blank row programmatically. `a`/`e`/`g`/`u` instead spanned 6 rows (rows 1-6), each because of one literally-duplicated row in its own bitmap data (e.g. `u` repeated its `10001` side-row four times instead of three) — almost certainly a copy-paste slip when those four glyphs were originally authored, not a deliberate design choice; the exact same "drop one duplicated row to compress by one" technique is already used correctly elsewhere in this same font for the accented uppercase letters (`Á`/`É`/etc.), which is what confirmed the fix direction. `á`/`é` shared the same buggy 6-row body as their unaccented counterparts and got the same fix (row 0 accent, row 1 blank, rows 2-6 body); `í`/`ó`/`ú`/`ü` were already independently authored with the correct 5-row body and needed no change. Verified by rendering the full lowercase alphabet with horizontal guide lines at each glyph row — all x-height letters now sit flush on the same baseline.
- **`FontGlyphs5x7`: lowercase `h`'s crossbar (row 3, `11110`) stopped one column short of its right leg, leaving the leg visually disconnected from the arch — fixed to `11111` (full-width crossbar), confirmed by rendering both versions large side by side.** User-reported ("se ve rara"), root-caused by rendering the glyph in isolation with row/column guide lines: the arch's rightmost column and the right leg's topmost pixel only touched diagonally at a corner, not a shared edge, reading as a floating/hanging leg rather than a connected letterform. `n` (and `ñ`, which reuses `n`'s body) has the exact same `11110`-then-`10001` transition and the identical flaw — left unchanged, not reported, pending confirmation before touching it too. Uppercase `H`'s crossbar is already full-width (`11111`); this was specific to the lowercase arch-shaped letters.
- **`Guide/Trail2D_Guide.md` added — quick, since `Trail2D` is a small, self-contained ring buffer with little non-obvious behavior beyond the per-segment fade tradeoff already reviewed.** Rounds out `Guide/` coverage for the 2D pieces reviewed this session (`Primitive2DBatch`, `Camera2D`/`ViewportAdapter2D`, now `Trail2D`) before moving on to `src/Core/`.
- **`PrimitiveInput.GetAxis`'s doc comment fixed — it claimed "positive wins a tie" but the code (and the actual Godot `Input.get_axis` it's modeled on) cancels to exactly 0 when both keys are held.** Confirmed by decompiling Godot's own behavior isn't needed here — its formula is public (`strength(positive) - strength(negative)`) and gives the same 0 on a tie; the phrase was almost certainly left over from an earlier tie-breaking implementation that got rewritten without updating the comment.
- **`PrimitiveInput` gamepad audit added 3 things, found by comparing the wrapped surface against MonoGame's actual `GamePad` API (checked the exact `SetVibration` overloads against the referenced 3.8.5.1 assembly via decompilation).** (1) `SetVibration(leftMotor, rightMotor, [leftTrigger, rightTrigger,] player = 0)` — was entirely missing; deliberately a raw, stateless passthrough with no built-in duration/pulse timer, since deciding *how long* to rumble is a per-game call (fixed pulse, decaying with trauma/distance, etc.), the same reasoning that's kept every other "system" out of this library. (2) `LeftTriggerDeadzoned`/`RightTriggerDeadzoned` — the sticks already had a `*Deadzoned` counterpart, the triggers didn't, a plain asymmetry with no reason behind it. (3) `IsAnyButtonPressed` — for a "press any button to join" lobby flow; deliberately checks only face/shoulder/stick-click/D-pad/Start/Back buttons, excluding the analog stick/trigger "as digital button" flags MonoGame also reports under `Buttons`, so idle stick drift or a light trigger rest can't misfire it. `GamePadCapabilities` (does this controller even support rumble/how many buttons) and a rescaled radial stick deadzone (vs. the existing hard-cutoff) were considered and left out — the former's need is already covered by `SetVibration`'s own bool return, the latter is real but adds complexity for a marginal analog-feel gain over the current simple cutoff. Wired into `InputPanelTest`'s gamepad panel (A rumbles for 0.25s, triggers show deadzoned, an indicator flashes on any button) so all three are visibly exercised, not just unit-testable in isolation.
- **`PrimitiveInput.IsAnyKeyPressed`/`IsAnyMouseButtonPressed` added, mirroring gamepad's `IsAnyButtonPressed`.** Straightforward: cache `Enum.GetValues` once (`Keys`/`MouseButton`) and check `IsKeyPressed`/`IsMouseButtonPressed` per value — ~150 keyboard checks per call is negligible next to a single draw call, so no need for anything cleverer.
- **`PrimitiveInput.GetCharPressed()` added, requiring a new `PrimitiveInput(GameWindow)` constructor — confirmed first that this genuinely can't be built from `Keys` polling, the way everything else in this class is.** `Keys` is physical key identity, not the character a layout/shift/dead-key combination produces (`Keys.Q` is `'A'` on AZERTY; this library's own `DebugFont5x7` supports Spanish accents like `'á'`, typically composed from a dead-key sequence only the OS resolves), and OS key-repeat timing can't be reconstructed by guessing at settings the OS already knows — confirmed even raylib's own `GetCharPressed` (the name and shape this mirrors) is backed by GLFW's real character callback internally, not raw key-state polling; only the "drain a queue each frame" *shape* is a polling-style API, not the substance. The new constructor subscribes to `GameWindow.TextInput` (checked its exact signature — `EventHandler<TextInputEventArgs>`, `Character`/`Key` fields — against the referenced 3.8.5.1 assembly via decompilation); the existing parameterless constructor is untouched and simply never populates the queue, so `GetCharPressed()` always returns `'\0'` on it rather than throwing — "returns nothing if not configured," per how it was asked for. The queue itself is a fixed 64-char ring buffer (not a growable `Queue<char>`), dropping new characters once full rather than growing unbounded if a caller never drains it — same bounded-capacity spirit as `Trail2D`'s own ring buffer. `PrimitiveInput` now implements `IDisposable` solely to unsubscribe the `TextInput` handler; non-breaking, since not calling `Dispose()` costs nothing for the overwhelmingly common case of one `PrimitiveInput` living the whole game. Verified via reflection (invoking the private event handler directly, since real `TextInput` events need actual OS focus/typing): FIFO order preserved through wraparound, and feeding 500 characters into a 64-capacity queue without draining recovered exactly 64 with no exception. Wired into `InputPanelTest` as a real "type here" box (Backspace/Enter polled separately via `Keys`, since those are editing controls, not typed content — the same split a real text box would make).
- **`PrimitiveInput` compared against raylib/love2d/Godot/Unity's input surfaces (not their action-mapping layers, deliberately out of scope) turned up 2 real gaps and 1 dead end.** The dead end first, since it's worth remembering: a proper "relative/captured" mouse mode (love2d's `setRelativeMode`, Godot's `MOUSE_MODE_CAPTURED`, Unity's `Cursor.lockState`) doesn't exist to wrap — decompiled MonoGame's `Mouse` class and confirmed it only has `GetState`/`SetPosition`/`SetCursor`/`WindowHandle`, no relative-mode toggle at all; the manual re-center-every-frame trick `SetMousePosition`/`ResetMouseDelta` already support **is** the real ceiling here, not a missing convenience method, since the underlying capability was never exposed by MonoGame in the first place. What MonoGame does have and this library hadn't wrapped: (1) `Mouse.SetCursor(MouseCursor)` — 12 built-in system shapes plus `MouseCursor.FromTexture2D` for a custom image; added as `PrimitiveInput.SetCursor`, matching `SetMousePosition`'s existing precedent of holding one-way mouse commands alongside the polling queries. (2) `KeyboardState.CapsLock`/`NumLock` — lock-key *state* (not physical key-down), useful for a custom on-screen keyboard or a password-field warning; added as two one-line properties.
- **`Guide/PrimitiveInput_Guide.md` added.** Covers keyboard/mouse/gamepad polling, `SetVibration`'s no-duration design, `GetCharPressed`'s `GameWindow` requirement, and — as its own explicit section — the relative-mouse-mode dead end above and why an action-mapping layer is deliberately out of scope, so the same question doesn't get re-litigated by a future reader.
- **`Palette`/`ColorUtil` audit added `ColorUtil.Invert`/`Contrast`, and closed a real regression-safety gap: `Saturate`/`Desaturate`/`Multiply`/`Screen`/`Overlay`/`Additive` had zero automated tests despite `DECISIONS.md` claiming their invariants were checked when added — that verification was ad-hoc and never became a permanent test.** `Invert` (`255-channel`, confirmed against Godot's `Color.inverted()`) and `Contrast` (RGB pull toward/away from the 127.5 midpoint, `amount` in [-1,1]) are the only two real gaps found comparing against raylib/love2d/Godot/Unity — everything else considered (per-color alpha replacement, normalized/packed-int conversion, an exhaustive named-color set, proper alpha-compositing) turned out to already exist on MonoGame's own `Color` struct (`new Color(color, alpha)`, `ToVector3/4`, `PackedValue`, `Color.AliceBlue` etc. — confirmed via decompilation) or be lower-value here since `Primitive2DBatch` already composites layers via its GPU blend state. Added 9 new `ColorUtilTests.cs` checks: the two new methods, the four previously-untested blend modes (including verifying alpha always comes from the first argument, not just documented), `Saturate`/`Desaturate`'s direction, `FromHex`'s alpha path/no-#/lowercase/error cases (previously only the plain 6-digit round-trip was exercised), and `Palette.Cycle`'s wraparound plus `All`/`Primary`/`GradientPairs` opacity. 80/80 passing.
- **`Guide/Color_Guide.md` added, covering `Palette` and `ColorUtil` together** (they're normally used together: pick from `Palette`, adjust with `ColorUtil`) — including an explicit "what's deliberately not here" section (auto-contrast text color, alpha-compositing, things MonoGame's own `Color` already covers) so the same raylib/love2d/Godot/Unity comparison doesn't get redone by a future reader.
- **`Noise` audit: verified the core algorithm line-by-line against Ken Perlin's own reference implementation — no bugs found — then moved `Fbm1D`/`2D`/`3D`'s `octaves`/`lacunarity`/`gain` from per-call parameters to instance properties (`Octaves`/`Lacunarity`/`Gain`, settable at construction or afterward), and added `RidgeNoise2D`/`3D` and `Turbulence2D`/`3D`.** The parameter-to-property move matches this project's own established shape for tunable knobs (`Camera2D.MoveSpeed`, `PrimitiveInput.DoubleClickTime`) rather than repeating the same three values at every call site of a terrain/effect that wants one consistent fBm configuration; construct a second `Noise` with the same seed (cheap — one permutation shuffle) if two different configurations sampling the same gradient field are genuinely needed. Only 2 real call sites needed updating (`NoiseTest`, one test) — confirmed via grep before making the breaking change. `RidgeNoise2D`/`3D` (`(1-|sample|)²` per octave — sharp ridgelines) and `Turbulence2D`/`3D` (`|sample|` summed — a softer, billowy look) are confirmed against Godot's `FastNoiseLite` (`FRACTAL_RIDGED`) and `stb_perlin.h` (the header most raylib users reach for), not invented; both naturally output roughly `[0,1]`, unlike `Fbm*`'s `[-1,1]`, documented explicitly. Deliberately did not add Godot-`FastNoiseLite`-style alternate noise TYPES (Simplex/Cellular/Value/domain-warping) — that's a whole multi-algorithm engine, out of scope; Ridge/Turbulence are just alternate octave-combination formulas over the existing `Sample*`, the same shape `Fbm*` already has. Added 5 new `NoiseTests.cs` checks: continuity (the core "smooth, not jittery" property no existing test actually verified), continuity across the negative-coordinate boundary (protects the `(int)Floor(x) & 255` lattice-wrap trick from a future "simplify to `% 256`" refactor, which would silently reintroduce a seam at every integer since `%` doesn't wrap negatives the same way), `Fbm2D`/`3D` determinism plus `Octaves=0` returning exactly `0`, Ridge/Turbulence's `[0,1]`-ish range, and a check that Ridge's squaring actually fires (its average should never exceed the unsquared `1-|n|` baseline, since squaring a `[0,1]` value can't increase it). Verified Ridge vs. Turbulence visually too (rendered side by side) — Ridge reads as sharp bright ridgelines, Turbulence as softer billows, confirming the two are actually distinct, not just numerically different. 85/85 tests passing.
- **`Guide/Noise_Guide.md` added.** Covers `Sample1D/2D/3D`, the `Fbm`/`RidgeNoise`/`Turbulence` families, the `Octaves`/`Lacunarity`/`Gain` property move, why `Sample1D` needs its own dedicated gradient, and — as its own section — what Godot's `FastNoiseLite` offers that this deliberately doesn't (alternate noise types, domain warping), so that comparison doesn't get redone later.
- **`Easing` audit: verified every formula against the canonical easings.net/Penner reference set — no bugs found — then added the two families it was missing, `Quint` (quintic, one step past `Quart` in the existing polynomial progression) and `Circ` (circular, a distinctly different quarter-circle-arc shape), confirmed against raylib's `reasings.h`, Godot's `Tween.TransitionType` enum, and DOTween's `Ease` enum all agreeing these are standard members of the same set every other family here already belongs to.** Built `tests/MonoPrimitives.Tests/EasingTests.cs` from scratch — the file had zero automated tests before this, despite being pure, trivially-testable math. Checks, run across all 31 functions via name/delegate tables rather than one-off asserts: every function's `f(0)=0`/`f(1)=1` boundary; every `InOut` variant's `f(0.5)=0.5` (verified analytically first for `Back`/`Bounce`/`Elastic`, whose overshoot/oscillation made this non-obvious, before trusting the test); monotonicity for the 7 smooth families (deliberately excluding `Back`/`Bounce`/`Elastic`, whose entire character is *not* being monotonic); ease-in-lags/ease-out-leads linear pace at `t=0.25` for those same 7; `Back`/`Elastic`'s defining overshoot-past-`[0,1]` and `Bounce`'s defining stay-close-to-`[0,1]`; and a universal `In(t) == 1 - Out(1-t)` identity across all 10 families (verified algebraically for `Back` before writing it as a test, then found to hold for all of them) — a single check that would catch a typo in almost any `*Out` function relative to its `*In`. Verified `QuintOut`/`QuintIn` and `CircOut`/`CircIn` visually too (plotted against `QuartOut`/`QuadOut` respectively) — Quint visibly more pronounced than Quart, Circ visibly a rounder, distinct arc shape, not just numerically different. 93/93 tests passing.
- **`Guide/Easing_Guide.md` added.** Covers all 11 families (including the new `Quint`/`Circ`), a "picking one" cheat-sheet by desired feel, and the two universal properties (`InOut`'s `f(0.5)=0.5`, `Out(t) = 1 - In(1-t)`) as their own explicit section since they're non-obvious and now permanent tests.
- **`UnitCircleLut.Sample` had a real, if small, imprecision for negative/out-of-`[0,1)` input — fixed by flooring before casting to an index, same pattern as `Noise`'s lattice wrap.** `(int)f` truncates toward zero, not floor, so for negative `f` the fractional part came out negative *after* the index was separately wrapped correct via bitmask — verified numerically this was NOT the index-mismatch bug it first looked like (a paper trace suggested the wrong table entries were being interpolated), because linear interpolation is linear in the continuous position regardless of which two adjacent entries you decompose it into; the actual error was just curvature (a chord instead of the true arc) accumulated across one extra lattice step, measured up to ~0.00015 in unit-circle units (~1.5px at a 10,000px radius) — real, but only very rarely user-visible. Fixed anyway since the fix is free (same cost, just floors first) and now exact to float precision (~0.0000002) instead of "close." Added `tests/MonoPrimitives.Tests/UnitCircleLutTests.cs` from scratch — the file had no tests before this: the four cardinal points, unit-length across many angles, continuity across the `t01=0/1` wrap, and this exact regression (`Sample(t)` now matches `Sample(t mod 1)` to float precision for negative/large inputs, not just approximately). 97/97 tests passing.
- **`FontGlyphs5x7`: built `tests/MonoPrimitives.Tests/FontGlyphs5x7Tests.cs` from scratch, locking in both fixes found earlier this session (the a/e/g/u x-height bug, `h`'s crossbar) as permanent regressions instead of one-off visual checks.** Every glyph's row-span is checked against the class it belongs to (x-height letters exactly rows 2-6, ascenders 0-6, `i`/`j`'s dot+gap+body, every uppercase letter/digit 0-6) — the exact property that was silently wrong for `a`/`e`/`g`/`u` and would have caught it immediately. Also covers the fallback hollow-box glyph, `AdvanceFor`'s scaling (caught a bug in the test itself while writing it — doubling `pixelSize` and `glyphSpacing` simultaneously isn't the same as scaling linearly in `pixelSize` alone, since spacing is added in raw units before the multiply), and `MeasureText`'s multi-line behavior. 106/106 tests passing. No new source bugs found on this pass — the two already-known ones were the only issues; this was about closing the "visual check, then never verified again" gap.
- **`Guide/DebugFont5x7_Guide.md` added**, covering `FontGlyphs5x7` (shared bitmap data) and 2D's `DebugFont5x7` (`DrawString`/`MeasureText`) together, including the row-span convention every glyph follows and the two bugs found/fixed this session.
- **`Collision2D` audit: verified every algorithm against its standard reference (PNPOLY for point-in-polygon, SAT for convex polygon overlap, Ericson's closest-point-between-segments for capsules) — no bugs found — then added the one real gap in the shape-pair matrix, `CheckCollisionCapsulePoly`/`CapsuleRec`/`CapsuleTriangle`.** Every other shape already has a `Rec`/`Triangle`/`Poly` counterpart (`CircleRec/Triangle/Poly`, `RecPoly/Triangle`) except Capsule, which — despite being a first-class shape with its own `Fill`/`Border`/`Draw` family and two existing collision checks (`CircleCapsule`, `CapsuleCapsule`) — had no `Rec`/`Triangle`/`Poly` counterpart at all. Not a raylib/Godot/Unity comparison (none of them expose a raw 2D capsule-collision math function the way this library already does for `CircleCapsule`/`CapsuleCapsule`) — this is completing this library's *own* already-established matrix, the same "audit your own method matrix" methodology already used successfully on `Primitive2DBatch` earlier this session. Implemented the same way `CheckCollisionCirclePoly` already generalizes to concave input (not SAT-based): true if either capsule endpoint is inside the polygon, or the capsule's own axis segment comes within its radius of any edge — the edge check alone also correctly catches a capsule passing all the way through solid material with both endpoints outside, since crossing into a closed shape always crosses one of its edges. Rounded out `Collision2DTests.cs`, which — like every other file audited this session — had roughly half its public surface untested, including the two most complex algorithms (SAT, the capsule segment-distance core) that `DECISIONS.md` already claimed were "hand-verified" but never became permanent tests: added coverage for every previously-untested method, plus a concave L-shaped polygon fixture (the union of two overlapping rectangles with one quadrant notched out) exercising `CheckCollisionPointPoly`/`CirclePoly`/`CapsulePoly` against a case a SAT-based check would get wrong, including the "capsule entirely within the notch" (no collision) and "capsule passing through solid material, both endpoints outside" (collision) cases specifically. Wired the 3 new methods into `CollisionTest` (keys `Y`/`U`/`I`) against the same concave star polygon mode 9/`CirclePolygon` already uses. 114/114 tests passing.
- **`Guide/Collision2D_Guide.md` added**, covering every overlap/ray check grouped by shape, the SAT-vs-non-SAT convexity distinction and which methods need which, and the concave-polygon test fixture as a worked example.
- **`RandomUtil` audit: verified every distribution against its standard reference (Marsaglia polar for Gaussian, Knuth's algorithm for direct Poisson, three-regime Binomial) — no bugs found in the sampling math itself — but the user caught a real gap this audit had missed: `private readonly Random _rng` meant a caller could reach only the methods this class chose to wrap, never `Random`'s own surface (`NextBytes`, `NextInt64`, `NextDouble`, .NET 8's `Random.Shuffle`), which had led me to wrongly dismiss adding a `Shuffle` wrapper on the reasoning that "`Random.Shuffle` already covers it" — true of the type, false of what was actually reachable.** Fixed by adding `public Random UnderlyingRandom => _rng` (and the equivalent on `Shared`, over `Random.Shared`), documented as sharing the exact same seeded stream rather than a second independent one — constructing a separate `new Random(seed)` instead would desync the moment either stream advances. Also added `NextWeightedIndex(ReadOnlySpan<float> weights)` (linear-scan cumulative-sum pick, O(n), throws on empty/negative/all-zero weights) after confirming it against Godot's `RandomNumberGenerator.rand_weighted_pick` — the one real gap comparing against raylib/love2d/Godot/Unity, everything else (shuffle, next-bytes, basic uniform/int ranges) already covered by `UnderlyingRandom` or existing methods. Built `tests/MonoPrimitives.Tests/RandomUtilTests.cs` from scratch (no prior coverage existed despite `DECISIONS.md` describing this class's algorithms as already verified): determinism, every distribution's sample mean/variance against theory over large sample counts (all three Poisson/Binomial regimes separately), `NextInsideUnitCircle`/`NextOnUnitSphere`/`NextInsideUnitSphere`'s area/volume-uniformity specifically (`E[r²]≈0.5` for the disc, `E[z²]≈1/3` and `E[r³]≈0.5` for the sphere — chosen because a regression to a naive uniform-radius or uniform-latitude approach would visibly miss these exact numbers), `NextWeightedIndex`'s proportions and error handling, `UnderlyingRandom` sharing the same stream, and a 16-thread concurrent stress test of `Shared`. Two bugs were caught in the test code itself, not the source: a `Parallel.For(0, 16, _ => ...)` lambda whose own `_` parameter collided with the discard assignments inside it (`CS0266`/`CS0029`, fixed by renaming the parameter to `taskIndex`), and an initial wrong expected value for the sphere's `E[r³]` (assumed `0.75`; the measured `~0.499` was correct, trusted over the paper guess, then re-derived — since `R=cbrt(U)` for uniform `U`, `R³=U` exactly, so `E[R³]=E[U]=0.5`). 128/128 tests passing.
- **3D audit begins: `TrigLut` (3D) first pass found the same class of bug `UnitCircleLut.Sample` had, in `SinCosStep`'s generic (non-exact-division) interpolation path — a raw truncating `(int)t` cast instead of a floor.** Confirmed numerically (a scratch console project comparing old vs. floored behavior against `Math.Sin`/`Cos` across `step` values on both sides of zero): for negative `step`, the truncating cast picks the wrong pair of adjacent table entries, ~3-4x the error of the floored version at the same inputs (both small — a curvature-accumulation error, not a wildly wrong result — but the floored version is exact to float precision where the old one was merely close). Fixed the same way `UnitCircleLut.Sample` was: floor before splitting into index+fraction. While there, closed a real API-consistency gap `ARCHITECTURE.md` had been silently overclaiming: it already described `TrigLut` as `UnitCircleLut`'s "3D counterpart," but `TrigLut` had no continuous-angle entry point matching `UnitCircleLut.Sample(t01)` — only `SinIndex`/`CosIndex` (raw integer index) and `SinCosStep(step, steps, ...)` (division of a circle), neither of which lets a caller sample an arbitrary angle not expressed as one of those. Added `TrigLut.Sample(t01, out sin, out cos)`, sharing its interpolation core with `SinCosStep`'s generic path (so the floor fix only needed to happen once). Then, per explicit request: added `SampleRadians`/`SampleDegrees` to both `UnitCircleLut` (2D) and `TrigLut` (3D), each backed by a precomputed `TurnsPerRadian`/`TurnsPerDegree` constant (`1/(2*PI)`/`1/360`) multiplied in, not divided by `2*PI`/`360` on every call. Verified numerically against `Math.Sin`/`Cos` across 20,000 random angles (including negative and >1-turn values): max error ~1.9e-5 for 2D's 512-entry table, ~5.6e-6 for 3D's 1024-entry table — both consistent with each table's own lattice-spacing error bound (error scales ~1/Resolution², so 3D's 2x resolution gives ~4x lower error), confirming `Resolution` doesn't need to change for this addition. `Resolution` itself (512 for `UnitCircleLut`, 1024 for `TrigLut`) was separately reconsidered on request and left as-is: at `Resolution=512` the worst-case error is ~1.9e-5 of a turn, which stays sub-pixel at any radius up to several thousand world units/pixels (the sane range for `Primitive2DBatch`'s shapes) — increasing it would trade table size/cache footprint for precision no one can see; decreasing it would start showing at moderate radii. Added a new `UnitCircleLutTests.cs` check for `SampleRadians`/`SampleDegrees` (agreement with the equivalent `Sample(t01)` call and with real trig, across negative/large angles); a dedicated `TrigLutTests.cs` for the 3D side, plus `Camera3D`/`DebugFont5x7` (3D)'s own audits, are the rest of this pass — `Collision3D` deliberately last per instruction.
- **`Camera3D` audit: verified every movement/rotation method formula-by-formula against raylib's own `rcamera.h` source (`CameraMoveForward/Up/Right/ToTarget`, `CameraYaw/Pitch/Roll`, `GetCameraForward/Up/Right`) — an exact match, including `Pitch`'s `lockView` pole-clamp, algebraically confirmed identical to raylib's independent two-`Vector3Angle`-call computation despite being computed here via a single `AngleBetween` call (`angle(-up,v) = PI - angle(up,v)` substituted in) — and found one real bug by comparing directly against its own sibling `Camera2D`, not against an external reference: `Camera3D.Reset()` never called `ResetTrauma()`, so `Trauma`/shake survived a reset when `Camera2D.Reset()` (the class this one is explicitly built to match) already clears it.** Fixed by adding the missing call, and updated `Reset()`'s own doc comment (which already claimed to clear "screen-shake state" — it didn't). Built `tests/MonoPrimitives.Tests/Camera3DTests.cs` from scratch (zero prior coverage, same gap `Camera2D` itself still has) — pure math throughout, including a `Viewport` struct (needs no `GraphicsDevice`) for a real `WorldToScreen`/`ScreenToWorld` round-trip and a center-screen `GetScreenToWorldRay` direction check. Caught the `Reset`/`Trauma` bug this way; also caught 3 bugs in the test code itself before trusting it: (1) asserted `Forward⊥UpNormalized` as a basis invariant, which is false in general — only `Right = cross(Forward, UpNormalized)` is guaranteed perpendicular to *both* its inputs by construction, `Up` is just a "roughly up" hint, not required to be exactly perpendicular to `Forward`; (2) an inverted condition on the `Yaw(rotateAroundTarget: false)` check — wrote `!Close(...)` where the intent (and correct behavior) was "Target changes, Position doesn't," but the condition as first written flagged the correct behavior as a failure; (3) called `SmoothZoom` with the same nonzero `delta` on every iteration of a loop, which its own doc comment explicitly warns against (each call adds onto the pending target immediately) — instead of a single discrete request followed by `delta=0` calls to advance the easing, exactly the "continuous input" misuse the doc comment names. All three were caught by the test actually failing and re-reading the assertion/API contract, not assumed away. 149/149 tests passing. `Guide/Camera3D_Guide.md` added.
- **3D `DebugFont5x7` (`DrawString3D`/`MeasureText3D`/`GetBillboardAxes`) audit: no source bugs found — `GetBillboardAxes`' right/up derivation was verified algebraically (general case, not just the level-camera example this session's summary first checked) and confirmed against the ground truth (the actual view matrix's own X/Y basis rows) via a new test, across 4 camera positions including one above and to the side of the labeled point.** That last case caught a bug in the TEST itself, not the source, the same class as `Camera3DTests`' earlier basis mistake: the first draft compared billboard `up` against `cam.UpNormalized` (just `Up` normalized, not orthogonalized against `Forward`) instead of the camera's true rendered up-axis — the two only coincide when the camera sits level with what it's looking at, and a camera above-and-to-the-side of its target is exactly the case where they diverge. Fixed by reading the actual basis off `GetViewMatrix()` (`M11/M21/M31` = right, `M12/M22/M32` = up) instead of hand-deriving what the "true" up should be. Built `tests/MonoPrimitives.Tests/DebugFont3DTests.cs` from scratch (needs a real `GraphicsDevice`, same as `ShapeTests3D`): the billboard-axis checks above, the straight-up/down pole fallback to `BuildBasis`, `DrawString3D` emitting geometry only for non-space glyphs (checked via `Primitive3DBatch.TrianglesSubmitted`, zero for space-only text, exactly double for a doubled glyph), `MeasureText3D` agreeing with the shared `FontGlyphs5x7.MeasureText`, and not-begun/null/empty/zero-`pixelSize` no-op paths. Extended the existing `Guide/DebugFont5x7_Guide.md` (already covered the shared glyph data and 2D's renderer) with a "3D" section rather than forking a new guide file, since it's the same font system rendered differently per namespace. 153/153 tests passing — this closes the `Camera3D`/`DebugFont5x7`(3D)/`TrigLut` trio explicitly requested; `Collision3D` remains deliberately last, after the rest of the 3D surface, per instruction.
- **`Primitive3DBatch`/`Primitive3DBatchShapes` full audit: method surface confirmed against raylib's `rmodels.h` (every 3D shape function it has — `DrawLine3D`/`DrawPoint3D`/`DrawCircle3D`/`DrawTriangle3D`/`DrawTriangleStrip3D`/`DrawCube(V)`/`DrawCubeWires(V)`/`DrawSphere(Ex)`/`DrawSphereWires`/`DrawCylinder(Ex)`/`DrawCylinderWires(Ex)`/`DrawCapsule(Wires)`/`DrawPlane`/`DrawRay`/`DrawGrid`/`DrawBoundingBox` — fetched and checked directly, not from memory) is already a strict superset, each already correctly unified into this library's own `Fill`/`Border`/`Draw` shape instead of raylib's own `Ex`/`V`/`Wires`-suffixed sprawl. No raylib-side gap exists.** The real gap was internal: 2D's spline family (`DrawSplineCatmullRom`/`BezierCubic`/`BezierQuadratic`/`Basis` plus 4 static `GetSplinePoint*` sampling functions) was only half-ported to 3D (`CatmullRom3D`/`BezierCubic3D` only) despite the 3D file's own section header already saying "2D library parity." Added `DrawSplineBezierQuadratic3D`/`DrawSplineBasis3D` and all 4 `GetSplinePoint*3D` static functions (mirroring 2D's exact per-type routing: `DrawSplineCatmullRom3D` still calls `Vector3.CatmullRom` directly rather than its own new static helper, matching 2D's own `DrawSplineCatmullRom` not routing through `GetSplinePointCatmullRom` either; `DrawSplineBezierCubic3D` was switched to call the new public `GetSplinePointBezierCubic3D` instead of a private duplicate `CubicBezier`, matching how 2D's own `DrawSplineBezierCubic` already routes through its public helper). Verified by rendering all three curve types together in a scratch project: Catmull-Rom visibly passes through its inner two control points, the new Basis curve visibly does NOT touch either of its control points (confirming the approximating-not-interpolating behavior its doc comment claims), and the new BezierQuadratic curve passes through its anchor points — not just reasoned about.
- **Two naming violations found against this project's own `CODESTYLE.md` rule ("no `Ex`/`V`-suffix for a parameter variant — it's an overload of the same name instead") and fixed.** (1) `DrawArrow3D(start, end, headRadius, sides, color)` existed as a completely separate method name alongside `DrawArrow`'s own overload family, despite its own doc comment admitting it was "a simpler, fewer-argument alternative to `DrawArrow`'s overload family" — exactly the pattern the rule forbids. Folded into `DrawArrow` as a new overload with the identical signature/behavior (no argument reordering, so every call site only needed the method name changed) — 2 call sites updated (`ShapeTests3D.cs`, `Gallery3D.cs`). (2) `DrawFrustumWires` was the only wireframe-only *closed* 3D shape not named `Border<Shape>` (every other one — `BorderCube`/`BorderSphere`/`BorderBoundingBox`/etc. — follows the convention regardless of whether a `Fill` counterpart exists or makes sense); renamed to `BorderFrustum`. It also had zero test coverage and zero example usage before this pass — added to `ShapeTests3D.cs`.
- **Doc-precision pass**: fixed `ResolveSegments(int,float,int)`'s confusing "uses origin distance heuristics off" doc comment (unclear grammar, not describing the actual behavior — it measures auto-LOD distance from the world origin instead of an explicit center) and `DrawTriangleStrip3D`'s doc comparing itself to `FillPolygon`'s 2D sibling — wrong comparison: `FillPolygon` triangulates (ear-clipping for concave input), it does not "submit geometry as given"; the accurate 2D sibling is `DrawTriangleStrip`, which (like this method) takes raw points and submits them as a strip with no triangulation at all. Corrected to reference the right method.
- **Confirmed, by direct vector-math simulation (not just reasoning), a real limitation in `DrawLineStrip3D`/`Trail3D.Draw`: both draw each segment as an independent camera-facing quad, so two segments meeting at a bend compute different offset directions for what should be one shared corner.** A moderate bend at `thickness=1.5` produced a 0.41-unit gap between the two segments' versions of the "same" point — clearly visible, not a rounding-level nitpick. `Trail3D.Draw` has the identical issue (it's the same per-segment `DrawLine3D` loop), so this isn't an isolated oversight in one method, it's a property of not having a joined-billboard-strip renderer at all — 2D's `BuildOutlineGeometry` solves the equivalent problem for flat screen-space geometry, but a 3D camera-facing version is a real, separate feature (consistent per-vertex offset direction shared between two differently-facing segments), not a small patch. Documented in `Design/ROADMAP.md` as a known, deliberate deferral rather than fixed in this pass — matches this audit's scope (find and fix confirmable gaps; a from-scratch joined-3D-ribbon renderer is new infrastructure, not a gap-closing addition). 156/156 tests passing.
- **`Collision3D` audit (last file of the 3D pass): every existing algorithm (Ericson's closest-point-between-segments for capsule-capsule, the nearest-face box-normal fix, the infinite-cylinder-plus-two-caps capsule raycast) checked against its own already-documented reference and found correct — no bugs. Added the 3 real gaps found.** (1) `GetRayCollisionTriangle`/`GetRayCollisionQuad` (Möller–Trumbore, quad as two triangles) — raylib parity: this class's own naming (`CheckCollisionSpheres`/`Boxes`/`BoxSphere`, `GetRayCollisionSphere`/`Box`) already mirrors raylib's collision module function-for-function, and raylib's `GetRayCollisionTriangle`/`GetRayCollisionQuad` (confirmed via a live fetch, not memory: both return the same `Hit`/`Distance`/`Point`/`Normal` shape this class's own `RayCollision3D` already has) were the two raylib has that this class didn't. `GetRayCollisionMesh` was NOT added — needs a `Mesh`/`Matrix`, out of scope per `PROJECT.md` (no model loading). (2) `CheckCollisionCapsuleBox` — completes this library's own established Capsule-vs-every-shape matrix (2D already has `CapsuleRec`; 3D's box is its direct analog), not a raylib gap (raylib has no capsule collision math at all, only capsule drawing). Segment-to-box distance has no simple closed form the way point-to-box does, so it's minimized via ternary search over the segment's parameter `t` — valid because distance-to-box is convex in `t` (a sum of per-axis clamped-linear terms), giving one minimum with nothing to get trapped in. All 3 additions verified via a scratch console project (19 checks: parallel/behind-origin/reversed-winding rays for the triangle test, both triangle halves for the quad, inside/far/reaching-via-radius/passing-through/exact-touching-boundary for the box) before writing permanent tests. `Collision3DTests.cs` also had real coverage gaps despite already existing (unlike every other 3D file audited this session, which started from zero): no `CheckCollisionBoxes`, no `CheckCollisionCapsuleSphere`, no basic `GetRayCollisionBox` hit case (only its large-scale regression test existed), and zero coverage at all for `GetRayCollisionCapsule` — the most novel algorithm in the file. All closed. 165/165 tests passing. `Guide/Collision3D_Guide.md` added — this closes the full 3D pass (`Camera3D`/`DebugFont5x7`/`TrigLut`/`Primitive3DBatch`/`Collision3D`) started earlier this session.
- **Guide-writing correction: `Camera3D_Guide.md` and `Primitive3DBatch_Guide.md` had drifted into narrating the audit itself (raylib formula-by-formula comparisons, "this session found X," test-writing anecdotes) instead of reading as plain end-user reference like the 2D guides.** User caught this directly and asked for the 2D guides' tone specifically. Rewrote both to keep only user-facing method descriptions and behavior notes, moving the audit narrative to this file where it belongs; trimmed two similar sentences in `DebugFont5x7_Guide.md`'s 3D section for the same reason. `Guide/Collision3D_Guide.md` (this pass) was written directly in the corrected tone from the start.
- **Full audit of all 15 `Guide/*.md` files against their source files' actual public API, requested explicitly ("deben exponer toda la API pública... no procesos internos pero sí casos de uso").** Ran via a background Explore agent (extract every public member per source file, diff against what each guide names) rather than by hand, given the number of file pairs. Found real, confirmable gaps in 10 of the 15 guides — genuinely undocumented public members, not just phrasing: `Camera2D`/`Camera3D.SmoothDamp` (both overloads, on both classes), `Camera3D.CreateDefault`/`DefaultNear`/`DefaultFar`/`CameraProjection`/the `ViewportAdapter2D` constructor overload/`ResetHeadBobbing`/`MaxShakeRoll`/`ShakeNoiseSpeed`, `FontGlyphs5x7.GetGlyph`/`AdvanceFor`, `Primitive3DBatch`'s constructor `maxVertices` parameter/`DefaultMaxVertices`/`AutoSegmentsMin`/`AutoSegmentsMax`/`PendingVertices`/`Capacity`/`DrawCalls`/`TrianglesSubmitted`/`LinesSubmitted`/both `ResolveSegments` overloads/`DefaultPointSize`, `PrimitiveInput.Update(float)`/`GetAxis`/`GetVector2` (the last two were named in passing but never actually documented as callable methods), and `TrigLut.Mask`. Also caught one real doc *inaccuracy* while at it: `Primitive3DBatch_Guide.md` claimed `BorderPlane`/`DrawPlane` had "all three forms" matching `FillPlane` — the source only has 2 (no normal-vector overload for the wireframe/combined methods), fixed to describe the real 2. Biggest gap: **`Trail3D.cs` had zero documentation anywhere** — no `Trail3D_Guide.md`, no mention in `Trail2D_Guide.md`, nothing; added a full "3D: `Trail3D`" section to the existing guide (same pattern as `DebugFont5x7_Guide.md` covering both namespaces in one file) rather than forking a new guide for what's a near-identical port. The same pass also removed the remaining internal-process narrative the agent flagged (`DebugFont5x7_Guide.md`'s "Bugs found and fixed this session" section — removed outright, since its useful content was already covered by the x-height-convention paragraph above it; smaller "verified X"/"empirically Y" phrases in `Camera2D`/`Easing`/`Noise`/`PrimitiveInput`/`RandomUtil` guides, rephrased as plain behavior/usage statements instead of narrating how they were confirmed). `Collision2D_Guide.md`/`Color_Guide.md`/`Primitive2DBatch_Guide.md`/`UnitCircleLut_Guide.md` came back clean — no changes needed.
- **Viewport-resize investigation: no bug in this library's resize handling — `BoxingViewportAdapter2D`/`ScalingViewportAdapter2D`'s `Scale`/`Offset`/`BoundingRectangle` already update correctly across a live resize** (confirmed by simulating one: `GraphicsDeviceManager.PreferredBackBufferWidth/Height` + `ApplyChanges()` mid-session, flipping a 1280x720 pillarboxed 800x600 virtual res to a 900x900 window — the letterbox correctly flips from vertical to horizontal bars with the right pixel boundaries, a `ScreenToWorld(WorldToScreen(x))` round-trip stays exact before and after, and a rendered check showed zero leftover-backbuffer artifacts). **Found one real, non-obvious gotcha instead: `GraphicsDevice.Clear()` ignores a narrowed `Viewport` and always clears the entire render target.** A boxed 3D scene that clears once for the full-window "bar" color, then calls `Clear()` again after `Apply()` narrows the viewport to give the "inside" a different color, silently wipes the bars too — confirmed by rendering: the second `Clear()` painted the *entire* backbuffer, not just the narrowed rect. `examples/test/ViewportTest` never hit this because its 3D scene uses the same color inside and outside (distinguished only by drawn geometry, never a second `Clear()`). The correct fix uses existing API, no library change needed: draw a rectangle sized to the current viewport instead of clearing again — an actual draw call *is* correctly confined to `Viewport`, unlike `Clear()` — verified by rendering: a translucent full-viewport `FillRectangle` right after `Apply()` respected the narrowed rect exactly, before and after a live resize. Documented on `ViewportAdapter2D.Apply()`'s own doc comment with the working recipe.
- **Circle-specific edge feathering evaluated (a thin vertex-alpha gradient ring at the boundary, the same halo technique `Fill*Shadow` already uses internally) and rejected — hits the same wall the general shader-based AA effort already ran into, confirmed by rendering rather than assumed from precedent.** `SegmentsForArc`'s auto-LOD picks a segment count from the raw `radius` *parameter* — it has no visibility into a `Camera2D`'s `Zoom`, since that's applied later via the transform matrix `Begin()` receives. A world-space circle that looks fine unzoomed can render visibly faceted once a zoomed camera magnifies it (confirmed by rendering: radius=40 world units — 55 segments, comfortably above the 48-segment floor — magnified 8x showed clear polygon notches). A feather ring is *also* ordinary local-space geometry, so it scales by the exact same unknown zoom factor: sized to look like crisp 1px AA at 1x zoom, it's still only ~1px wide after an 8x zoom and does nothing to hide the (now 8x-magnified) chord error; sized wide enough to actually mask that chord error, it renders as a visible soft blur/glow around the shape, not a clean anti-aliased edge — both confirmed side by side in the same rendered comparison. The real fix for a circle known to be viewed zoomed-in already exists and needs no new API: `FillCircle(center, radius, segments, color)`'s explicit-segment overload lets a caller pick a higher segment count than the un-zoomed auto-LOD would, independent of `BorderCircle`/`DrawCircle` (which don't have an explicit-segment overload — a real, minor gap, not fixed here since it wasn't asked for).
- **`Camera2D`/`Camera3D.SmoothZoom`'s "settled" epsilon changed from a fixed `0.0005f` to `Min(0.0005f, |target| * 0.001f)`, verified necessary via simulation before touching it (per explicit instruction: fix only if actually warranted).** A fixed absolute epsilon compares fine for the ~0.1-10 default `Zoom` range, but `MinZoom`/`MaxZoom`/`MinDistance`/`MaxDistance` are public and caller-set — for a much smaller range (e.g. `0.001`-`0.01`), `0.0005` is a large *fraction* of a realistic per-tick delta, so `SmoothZoom` marked itself "settled" (clearing `_pendingZoomTarget`, after which it's a no-op with `delta=0`) while still measurably short: simulated 7% short of target after 1 frame for a `0.001`-`0.01` range, 27% short for a `0.00001`-`0.0001` range — and since settling permanently stops the ease, the camera never got any closer. The relative term only kicks in below the point where `0.0005` was already the smaller value, so every normal-range case (simulated: default range, near both clamps, a fast `ZoomSmoothTime`) settles in the same number of frames as before, within simulation noise.
- **`FrameLimiter` added to Core: sleeps most of a frame's remaining time, then busy-spins the last ~2ms.** The open question was whether `Thread.Yield()` or `Thread.SpinWait` finishes the final stretch more precisely than a plain busy loop — measured directly (a real `Stopwatch`, 300-frame runs at a 16.667ms target, percentile breakdown): all three are statistically identical (same median, same p95/p99). What actually drives precision is whether `Thread.Sleep` gets called at all: on Windows, any `Sleep` call has a real ~1-5% chance of running nearly a full extra frame long regardless of requested duration, `timeBeginPeriod(1)`, or which tail strategy follows it (median stayed exactly on target either way; p95/p99 jumped from ~16.7ms to ~29-30ms only on the Sleep-using variants) — a genuine OS scheduler-jitter cost, not something this class's own code controls. A pure busy-spin has zero such risk but pins a full core the whole frame, which is the wrong tradeoff for a general-purpose prototyping library; `FrameLimiter` sleeps the bulk of the frame and accepts the rare jitter, same as most real engines' Sleep-based limiters.
- **`FastTexture` added to Core: a `Texture2D`/`RenderTarget2D` wrapper that uploads via raw `glTexSubImage2D` (resolved through `SDL_GL_GetProcAddress`, falling back to the system GL library) instead of `SetData`, transparently falling back to `SetData` itself when the fast path can't be established (non-GL backend, renamed MonoGame internal, unsupported format).** A first synthetic benchmark (`TextureUpdateBenchmark.cs`, since deleted) found the raw path 2-4x *slower* than `SetData` and was reported as such — that result was an artifact of its own flawed methodology: 10 back-to-back calls inside one headless `Draw()` with no `Present()`/frame boundary between them forces the GL driver to fully serialize repeated writes to the same texture (a resource-hazard stall), which real per-frame usage never hits since GPU work between uploads hides the latency. Reproduced both results in the same process to confirm the methodology, not the technique, was at fault. Re-measured with a real per-frame MonoGame loop (2500x2500, once per real frame): raw upload is consistently ~2.5-2.7x *faster*. `FastTextureTests.cs` covers correctness only (round-trip, sub-rectangle, wrong-size throws, `Dispose`/`ownsTexture`, and the specific MonoGame texture-slot-cache hazard `InvalidateDeviceTextureCache` exists for — no contamination was reproducible via `SpriteBatch`'s actual draw path even with invalidation off, but the guard is cheap to keep) rather than a live perf number, since a single-shot headless harness is exactly the unrealistic access pattern that produced the wrong number the first time.
